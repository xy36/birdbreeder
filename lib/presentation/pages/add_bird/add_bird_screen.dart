import 'package:flutter/material.dart';

class AddBirdScreen extends StatelessWidget {
  AddBirdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Add Bird')),
    );
  }
}
