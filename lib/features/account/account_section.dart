/// A section of the account screen that can be navigated to directly.
///
/// Passed as a route argument so callers elsewhere in the app can send the user
/// to a specific setting instead of the top of a long, scrolling page.
enum AccountSection {
  cloudBackup,
}
